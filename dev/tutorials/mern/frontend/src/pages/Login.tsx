import { Header } from '../components';

const Login = () => {
  return (
    <main
      id='login'
      className='bg-cover bg-center bg-no-repeat'>
      <Header />
    </main>
  );
};

export default Login;
